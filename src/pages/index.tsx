import DefaultLayout from "@/components/layout/main";

const IndexPage = () => {
  return (
    <DefaultLayout title="Home">
      <section>
        Este es un home de ejemplo en el que se pueden integrar datos iniciales
      </section>
    </DefaultLayout>
  );
}

export default IndexPage